# Codex workspace notes

This directory is reserved for repository-local AI instructions and reusable
development context. It intentionally contains no machine-specific paths,
credentials, transcripts, or generated state.

Project rules belong in `AGENTS.md`; architecture decisions belong in `docs/`.
